server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true  # ????????????? ????????? ???????? ?? ?????? ?????????????????? ? Eureka ????????
      routes:
      # ?????? ??????? ???????? (???????????)
      # - id: user-service
      #   uri: lb://user-service   # lb = load-balanced ????? Eureka
      #   predicates:
      #     - Path=/api/users/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # ????? ?????? Eureka Server
  instance:
    prefer-ip-address: true
    # ????????????? (?? ???????????, ?? ???????):
    # hostname: localhost
    # non-secure-port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics  # ??? Actuator
  endpoint:
    health:
      show-details: always